<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ChatGPT" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
    <style name="TextStyle">
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="SendBtnStyle">
        <item name="android:src">@drawable/up_send</item>
        <item name="android:background">@drawable/send_btn_background</item>
    </style>
    <style name="MessageBgStyle">
        <item name="android:background">@drawable/rounded_corner</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/white</item>
    </style>
    <style name="RelativeBgStyle">
        <item name="android:background">@color/black</item>
    </style>
</resources>