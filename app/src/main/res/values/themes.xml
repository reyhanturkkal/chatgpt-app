<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ChatGPT" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ChatGPT" parent="Base.Theme.ChatGPT" />
    <style name="TextStyle">
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="SendBtnStyle">
        <item name="android:background">@drawable/send_btn_background_dark</item>
        <item name="android:src">@drawable/up_send_light</item>
    </style>
    <style name="MessageBgStyle">
        <item name="android:background">@drawable/rounded_corner_light</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/black</item>
    </style>
    <style name="RelativeBgStyle">
        <item name="android:background">@color/white</item>
    </style>
</resources>